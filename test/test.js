var assert = require('assert');
var isMinified = require('..');

describe('is-minified', function() {

  it('should do something awesome', function() {
    assert.equal(isMinified(), true);
  });
});
